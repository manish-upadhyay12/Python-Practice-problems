#  1. we can change value of variable after initialization 
# 2.   int can be conver into float
#  3.  stirng can be converted in int if it is a valid integer number 
#  4.  float value can be converted into (int)
#   5. string can hold everything in double code 
# 6. all data type to boolean 
    #    7 type convert in false 
    #     1. False 
    #    2.   0
    #    3. 0.0
    #    4. " "
    #    5  () = anti double
    #    6. { } = anty distionary
    #    7 [ ] = empty test

#  7 . when we take input so we print always a string is called formated string
#    it also means we use variable inside string 
#       name  = "manish"
#       age = 20
#           like   = print(f"hi my name is {name} and my age is {age}")
#   we write here (f) for formated string
#  formated string want that use f in starting other wise it take variable as a string

"""
#8. input method return the value but we have to save it so save it in a variable 
  9. bodmass
     bracket = ()
     ** Exponent (right to left = 2**3**5 = 2**(3**5))
     // ,/,*,%  - floor division, division, multiplication , modules
     +,- addition, substraction

     """

   #  10.In control flow statement we usally  use indentation (:)
   #11 for i in range(len(a)): here we have to give len of string in raenge to iterate the variable 
    # print(f"{i} : {a[i] }")   # if we write direct string variable so and print itertable value so it will be print whole string 
       # if we write string in place of range and print a[i] so  it give error 
       # if we gave range so string so we write a[i] 